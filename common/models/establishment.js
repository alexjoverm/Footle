module.exports = function establishment(Establishment) {

  /**
   * It says hello
   * @param {string} str pepe
   * @param {object} languague no desc
   * @param {geopoint} dummy
   * @param {Function(Error)} callback
   */
  Establishment.sayHello = (str, languague, dummy, callback) => {
    // TODO
    callback(null)
  }
}
